# 專案共同規格

## 預設流程

每次開始處理本專案任務時，必須先閱讀並遵守：

1. `AGENTS.md`
2. `.agents/skills/` 中與任務相關的 skills

實作前必須先檢查 `.agents/skills/`，確認是否有適合當前任務的 skill。若有，需先閱讀該 skill 的 `SKILL.md` 並依其規則執行。

## 專案定位

本專案為前端實作，目標是建立 momo 風格的 Merchant Card Showroom。

本專案需求、功能規劃、路由設計、實作步驟與驗收標準，以 `docs/PLAN.md` 為準。若其他文件或口頭描述與 `docs/PLAN.md` 衝突，需先向使用者確認。

## 回覆語言

- 一律使用繁體中文回覆。
- 專有名詞、技術名詞、套件名稱、元件名稱、路由名稱、API 欄位、程式碼識別字維持原文。

## 全域限制

- 不呼叫真實 momo API。
- 不爬取或依賴 momo production service。
- 不使用 momo 專有素材。
- 所有商品資料需使用 mock data。
- 作品應保持可維護、可說明、可驗證。

## 工程原則

- 優先完成小而完整的功能，不追求大而鬆散的範圍。
- 優先遵守既有專案結構與 `docs/PLAN.md` 命名。
- 實作前需先檢查 `.agents/skills/` 是否有適用的 skill。
- 只修改與任務直接相關的檔案。
- 元件需盡量保持可重用與低耦合。
- 狀態管理需集中且可追蹤。
- 資料存取需隔離在 service layer，避免散落在元件中。
- 文件需說明設計取捨、架構與後續演進方向。

## Agent 協作原則

- 修改前先確認目前目標與範圍。
- 不擴張未被要求的功能。
- 若需要取捨，優先保留可展示、可驗證的核心流程。
- 若實作與 `docs/PLAN.md` 衝突，先更新或說明 plan，再進行程式修改。

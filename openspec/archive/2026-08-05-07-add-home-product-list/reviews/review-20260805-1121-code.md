# Code Review — 07-add-home-product-list
**Reviewer:** codex
**Date:** 2026-08-05 11:21

## Summary
ready to archive

## Spec Compliance
- 首頁載入並顯示商品列表: met — `HomeView.vue` 透過 `useProductStore()` 載入商品，使用 `displayProducts` 與 Vuetify grid 顯示多張 `ProductCard`。
- 首頁商品卡可導向商品細節頁: met — `ProductCard` 的 `cta-click` 由首頁處理，導向 `product-detail` route 與對應商品 `id`。
- 基本狀態畫面: met — 首頁提供 loading skeleton、empty state、error alert 與 retry button。
- header 與 search bar 視覺: met — 首頁提供 header 與 read-only search bar，未實作真搜尋或遠端查詢。
- 可驗證性: met — `npm run test` 與 `npm run build` 通過，`/` 與 `/product/mock-air-fryer-001` 本機路由回 200。

## Findings
### Blocking
- 無

### Should Fix
- 無

### Nice to Have
- 後續導入 component/router 測試後，可補首頁 loading、empty、error 與 CTA 導覽的自動化覆蓋。

## Recommendation
archive

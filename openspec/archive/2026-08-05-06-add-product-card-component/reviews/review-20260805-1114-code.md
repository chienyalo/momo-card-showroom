# Code Review — 06-add-product-card-component
**Reviewer:** codex
**Date:** 2026-08-05 11:14

## Summary
ready to archive

## Spec Compliance
- 可重用 ProductCard 元件: met — `src/components/ProductCard.vue` 已建立，使用 typed `product: ProductCard` 與 `mode?: 'list' | 'detail'` props，並由首頁與細節頁分別以 list/detail mode 使用。
- momo-like 商品卡視覺: met — 元件包含白底卡片、固定比例商品圖、紅色價格、原價刪除線、促銷 badge、兩行商品名稱與 CTA button。
- 安全處理文字與缺省資料: met — 商品名稱使用兩行截斷，badge/promotion text 以條件渲染處理空字串，圖片提供 error fallback。
- 使用既有 schema 與 helper: met — 元件引用 `ProductCard` type、`formatPrice()` 與 `calculateDiscountRate()`。
- 可驗證性: met — `npm run test` 與 `npm run build` 通過，`/` 與 `/product/mock-air-fryer-001` 本機路由回 200。

## Findings
### Blocking
- 無

### Should Fix
- 無

### Nice to Have
- 後續導入 component test 工具後，可補 `ProductCard.vue` 的 mode rendering、長文字截斷 class 與缺省 promotion/badge 狀態測試。

## Recommendation
archive

# Code Review — 04-add-product-store
**Reviewer:** codex
**Date:** 20260805-1054

## Summary
ready to archive

## Spec Compliance
- 商品狀態必須集中由 Pinia Store 管理: met — `src/stores/productStore.ts` 已匯出 `useProductStore` Setup Store，並提供 `products`、`isLoading`、`error`、`editedProducts`。
- Store 必須透過 mock service 載入商品: met — `loadProducts()` 呼叫 `getProducts()`，並正確處理 loading、success 與 error state。
- 顯示資料必須合併 base data 與 edited data: met — `displayProducts` 與 `findProductById()` 使用 base product + draft merge，且測試驗證 base products 未被 mutate。
- Store 必須提供 storage hydrate/save actions: met — `hydrateFromStorage()` 與 `saveToStorage()` 已實作並有 Vitest 覆蓋。
- Product store 必須有 Vitest 覆蓋: met — `src/tests/productStore.test.ts` 覆蓋載入、錯誤、draft merge、reset、hydrate/save，`npm run test` 通過。

## Findings
### Blocking
- 無

### Should Fix
- 無

### Nice to Have
- 第 10 步實作 persistence utility 時，可把目前 store 內的 storage key 與 JSON parse 邏輯移出 store，讓壞 JSON fallback 與 migration 更集中。

## Recommendation
archive

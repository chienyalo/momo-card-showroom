# Code Review — 03-add-mock-product-service
**Reviewer:** codex
**Date:** 20260805-1044

## Summary
ready to archive

## Spec Compliance
- 商品資料必須由 mock service layer 提供: met — `src/services/mockProductService.ts` 已匯出 `getProducts()`，回傳 `Promise<ProductCard[]>`，內部 mock array 未匯出。
- 商品查找必須有明確找不到結果: met — `getProductById(id)` 找到時回傳商品，找不到時回傳 `null`，未以 runtime error 表示一般查無資料。
- Mock service 必須保留未來 API 替換彈性: met — service functions 皆使用 async / Promise boundary。
- Mock service 不得依賴 momo production service: met — mock data 使用 `picsum.photos`，測試驗證資料不含 momo production domain。
- Mock product service 必須有 Vitest 覆蓋: met — `src/tests/mockProductService.test.ts` 覆蓋列表、查找、查無、reference isolation 與 production domain 限制，`npm run test` 通過。

## Findings
### Blocking
- 無

### Should Fix
- 無

### Nice to Have
- 後續若 mock data 欄位增加，可考慮把 service response 型別抽成 `ProductService` contract，讓 Pinia store 對 service implementation 更容易替換。

## Recommendation
archive

# Code Review — 02-add-product-card-schema
**Reviewer:** codex
**Date:** 20260805-1025

## Summary
needs fixes

## Spec Compliance
- 商品卡資料結構必須集中定義: met — `src/types/productCard.ts` 已匯出 `ProductCard`，欄位包含 spec 要求的展示欄位。
- 商品卡 helper 必須提供穩定輸出: partial — `formatPrice()` 與 `calculateDiscountRate()` 對價格輸入有穩定 fallback，但 `normalizeProductCard()` 對部分文字欄位缺省覆蓋處理不足。
- Schema helper 必須有 Vitest 覆蓋: met — `src/tests/productCard.test.ts` 覆蓋價格格式、折扣計算與主要缺省行為，`npm run test` 通過。

## Findings
### Blocking
- 無

### Should Fix
- `src/types/productCard.ts`: `normalizeProductCard()` 先展開 `...input`，但未重新 fallback `id`、`imageUrl`、`discountBadge`、`promotionText`；顯式傳入 `undefined` 時會覆蓋預設值，與缺省欄位需穩定輸出的需求不完全一致。

### Nice to Have
- `src/tests/productCard.test.ts`: 可補一個測試涵蓋 `id`、`imageUrl`、`discountBadge`、`promotionText` 被傳入 `undefined` 的 fallback 行為。

## Recommendation
fix-first

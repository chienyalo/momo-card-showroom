# Tasks

- [x] 建立 `src/types/productCard.ts`。
- [x] 定義 `ProductCard` interface。
- [x] 在 `ProductCard` interface 加入 `id`、`title`、`imageUrl`、`price`、`originalPrice`、`discountBadge`、`promotionText`、`rating`、`soldCount`、`ctaLabel`。
- [x] 實作 `formatPrice()` helper。
- [x] 實作 `calculateDiscountRate()` helper。
- [x] 定義缺省欄位或無效輸入的 fallback 行為。
- [x] 建立 Vitest 測試檔，覆蓋 schema helper。
- [x] 測試 `formatPrice()` 的價格格式化行為。
- [x] 測試 `calculateDiscountRate()` 的折扣計算行為。
- [x] 測試缺省欄位或無效輸入處理。
- [x] 執行 `npm run test` 驗證 schema helper 測試通過。
